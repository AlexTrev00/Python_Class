métodos de listas 
list.append(elemento)
-pega un elemento al final de la lista

list.extend()
-añade elementos de una segunda lista al final de la primera lista

list.insert(pos,elemento)
-inserta un elemento en la posicion pos

list.remove(elemento)
-borra el primer elemento de la lista

el=list.pop(pos)
-donde list.pop(pos) es contenido de la variable el borra y regresa el elemento en la posicion pos

el=list.pop()
- borra y regresa el ultimo elemento de la lista

list.clear()
- borra todos los elementos de la lista

pos=list.index(elemento[,ini,fin])
- regresa la posicion de la primera instancia del elemento 

x = list.count(elemento)
- cuenta el numero de ocurrencias del elemento

list.sort(key=Nnoce,reverse=false)
- ordena una lista en orden ascendente/descendente 

list.reverse()
invierte el orden de los elementos en una lista

del list[i]
- elimina el elemento i de la lista

del list[:]
- elimina todos los elementos de la lista 

del list[i:n]
- eliminara los elementos desde i hasta n-1 
